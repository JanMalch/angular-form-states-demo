export interface ErrorStateMatcherOptions {
  immediate: boolean;
  useControlTouched: boolean;
  useControlDirty: boolean;
  useFormTouched: boolean;
  useFormDirty: boolean;
  useFormSubmitted: boolean;
}
